# Recon Wave 8 — Studio de vídeo (etapa 7 `edit`): editor estável, legendas backend/frontend

Reconhecimento único (read-only) para a Wave 8. Alvo: etapa 7 `edit` (plugin `studio/etapas/edit/`, serviço `studio/edit/`). Branch `develop` em `f080f1f`. Plano aprovado: `.claude/plans/2026-08-29-studio-de-video-estavel.md` (diagnóstico com file:line já está lá; este recon não o repete, complementa). Tudo é `[extensão]` da aula 014 (ADR-030).

## TERRENO

**Domínio `edit` não tem HLD** (só `docs/domains/edit/prd.md`, `features/edit-fdd.md`, `features/editor-video-completo-fdd.md`, `diagrams/mermaid/edit-fluxo.md`, `postman/`). O HLD que vale é o do `studio` (`docs/domains/studio/hld.md`): plugin de duas peças (`META`+`router.py`+`view.html`+`view.js`, descoberto por `studio/etapas/__init__.py:discover`), núcleo (`app.py`, `steps.py`, `web/*`) editável **só por frente de preparo/shell** (hld.md:64-72).

### A) `view.js` (1051 linhas, módulos internos por comentário `// ====`)
| Módulo | Linhas | O que importa para a wave |
|---|---|---|
| Store | 58-85 | `St` (timeline, selection, zoom, panel, rightTab, history/future). `commit(label, mutator)` :79-82 → `renderAll()` :406 → `renderRoot()` :409-419, que refaz `r.innerHTML = headerHTML()+bodyHTML()+timelineHTML()` e religa tudo (`bindHeader/bindLeft/bindPreview/bindTimeline/bindPointer`, `mountAudio`, `fit`, `stageBox`, `renderPanel/Props/Timeline/Preview`). `undo/redo` :84-85 idem. `snapshot()` :83 é o caminho "sem render" usado por sliders. |
| Model | 87-154 | `ET` :47 (t_txt/t_cap/v2), `segments()` :88-113 (posicional × sequencial), `tracks()` :128-149 monta as 6 faixas: item MÚSICA tem `uid:"music"` :144; SFX `uid:"sfx_${i}"` **por índice** :147. `findItem(uid)` :151. `clipFx(cid)` :153. |
| Playback | 156-272 | `videoPool` (por `file`), `sfxPool`, `musicAudio`; `attachPool()` :172 reancora no palco após `renderRoot`; `mountAudio()` :179 limpa `sfxPool` dos que saíram (padrão a copiar para o pool de overlays); `loopTick()` :238-254 chama `renderPreview()` por frame; `syncMusic()` :256-263 tolerância 0,3 s e `readyState>=2`; `seekTo()` :264-271. |
| Preview | 274-353 | `stageBox()` :275; `cssFilterFor(fx)` :282-293 (só blur/glow/sharpen/vignette); `tfCss` :294; `renderPreview()` :295-315 (aplica filtro só no `<video>` do V1); `renderLayers(stage)` :316-345 **remove e recria** `.ved-layer` a cada chamada; overlay de vídeo :328 cria `<video>` novo e seta `currentTime` antes do metadata. |
| Persistence | 355-405 | `save()` :358 PUT + `adopt()` :370-391 (preserva identidade dos objetos — qualquer reconciliação nova deve respeitar isso); `serialize()` :392-396 manda `editor: ed()` inteiro; `load()` :397-405 lê `ui.zoom/snap`. |
| Header/Body | 422-507 | kick "Etapa 7 · Montagem" :428; guia modal "Montagem no ritmo — aula 014" :458; `edFull` :439 (lugar do botão "⇤ Menu"). |
| Panels | 509-590 | `renderPanel()` :511 despacha por `St.panel`; `pMedia` :516-535 (botão `.mv2` "→ VÍDEO 2" :524-525; dblclick → `addMediaItem` :539-544, vídeo → V1); `pCaptions` :550-557 (`#capGen` hoje só `toast` :553); `pAudio` :558-567 (sem remover). |
| Props | 592-757 | `propsItem` :632-649; `propsBasic` :652-664; `propsAdjustTab` :704-709 (aceita `it.item.filters`); `propsTextBody` :710-730; helpers `bindNum/bindSlider/cset` :753-755 (usam `snapshot`+`renderPreview/renderTimeline`, sem `renderRoot` — modelo do "render incremental"). |
| Timeline | 759-849 | `timelineHTML()` :760-779; `bindTimeline()` :780-808 (scroll sync heads :791, drop por lane :802-807); `renderTimeline()` :811-824 troca só `innerHTML` de `#edRuler/#edTlHeads/#edTracks`; playhead height :820. |
| Interações/Ações | 851-966 | `deleteItems` :922-926 (`music` cai no vazio; guarda `clips<=1` :924; `toast` dentro do mutator); `rippleDelete` :927-932; `addText` :933-937 (caption em `t_cap`, y .82); `addOverlayVideo` :939-942; `clipParaV2` :943; `toggleEffect/setFilter/adjustTarget` :960-962. |
| Menu/Export/Shortcuts/Layout | 969-1038 | `openMenu` :969-978; `startRender` :998-1001 (subtítulo "Montagem no ritmo (ffmpeg)" :1000); `fit()` :1022-1028 lê `.side`; `bindResizers` :1029-1038 (timeline clamp 150–520, não persiste). |
| Ciclo de vida | 1040-1050 | `init/onProject/destroy` — `destroy()` é o ponto para remover `side-hidden`. |

`view.html`: CSS escopado `.ved*`; `.ved-stage video,.ved-stage img.base{position:absolute;inset:0;…}` :137 (pega o `<video>` da camada); `.ved-timeline{height:262px;min-height:150px;max-height:520px}` :205; `.ved-tl-main{overflow-y:hidden}` :223; `@media(max-width:1180px){.ved{left:0}}` :250; fallback `header.stephead` :256-259 (`eyebrow` "Etapa 7 · aula 014 · editor [extensão]", `h2` "Montagem de vídeo"); `<section id="guide" class="guide ved-fallback">` :262 (string exata fixada por teste).

### B) Fluxo de persistência e render (backend)
- `PUT /timeline` (`router.py:104-112`) → `edit.save_timeline` (`service.py:343-347`) → `validate_timeline` (`:191-282`; clips vazios OK `:195-197`; `music.file=None` OK `:242-249`; `start` livre `:224-229`) → `ed.normalize_editor` (`editor.py:389-427`) → `write_timeline` → resposta `{created:false, duration, timeline: decorate()}` (`service.py:350-355`).
- `editor.py`: `normalize_item` `:247-294`; branch `text/caption` `:261-265` guarda só `text/style/transform/anim` (descarta `effects/filters/presetCss` — e descartaria `words/mode/hi/chunk`); `normalize_style` `:159-176` (`color`/`bg` = string ≤32, sem regex); `ui` só `zoom`+`snap` `:425-426` (`normalize_ui_zoom` `:344-353`). Helpers reutilizáveis: `_s/_b/_num/_clamp/_clampi` `:79-110`, `safe_rel` `:118-127`, `normalize_effects` `:179-190`, `normalize_filters` `:193-200`. Limites: `MAX_ITEMS=4000` `:46` (legendas por palavra/janela contam aqui), `MAX_TEXT=5000`.
- Render: `render.start_render` (`render.py:342-434`) → `validate_timeline` → **422 "timeline sem clipes"** `:356-357` → master exige `music_path` (409 `NO_MUSIC`) `:358-361` → thread `run`: `burnin.render_layer_pngs(root, editor, 1920, 1080, root/"edit"/"_overlays")` `:387` (só `master`; falha vira aviso, não derruba `:391-393`) → `build_filtergraph(..., overlays=)` `:394-396`. No filtergraph: cada overlay é um `-i <png>` `:242-245`, `scale=1920:1080` `:278` e `overlay=0:0:enable='between(t,ini,fim)'` `:279` encadeado (`vov{k}`). Não existe caminho `ffconcat`/faixa; PNG é sempre full-frame. `_clip_fx_chain` `:106-140` aplica efeitos **só em clipes V1** (blur/sharpen/grain + eq/hue). `_overlays` é apagada após o render `:418,422`. Job: `registry = JobRegistry()` `:32`, `registry.start(pid, total, run, target=, output=, duration=)` `:434` (RuntimeError→409 se já roda), status `render_status` `:450`, arquivo `jobs/edit_render_<stamp>.json` `:437-447`.
- `burnin.py`: `_find_font` `:29-37` (Liberation/DejaVu/Arial — "Bricolage Grotesque" não existe para o Pillow; `style.font` é ignorado), `_font(size,bold)` `:40-47`, `_hex(color,default)` `:50-57`, `_text_png(item,W,H,path)` `:60-102` (tamanho = `style.size/1080*H*scaleX`, sombra, fundo, rotação), `_image_png` `:105-131` (**só imagem**: mp4 em `Image.open` falha silenciosamente → MP4 na V2 nunca entra no master — FDD §3 "fase 3"), `render_layer_pngs` `:134-160` (ordem overlay→caption→text, ignora `visible:false`, exige `end>start`, nome `layer_{n:03d}.png`, spec `{path,start,end}`).
- `common/ffmpeg.py`: `FFMPEG/FFPROBE` resolvidos na importação por `_find` (`shutil.which` ou `~/.local/bin`) `:10-16`; `available()` `:19`; `run(args, timeout)` prefixa `-hide_banner -y` `:23-30`; `probe` `:33-50`. **Não há helper de extração de áudio** — para o whisper: `ff.run(["-i", src, "-vn", "-ac", "1", "-ar", "16000", out])`, atrás de `ff.available()` (padrão 409 `NO_FFMPEG`, `router.py:15`).

## DECISOES VIGENTES
- **ADR-030** (Aceito, `ADH-OS-20260828-30`) — editor completo é extensão aditiva: bloco `editor` opcional, backbone (`clips/blacks/music/sfx/fade_out/loudnorm`) é a fonte do render; preview no browser, render fiel no ffmpeg, "nunca simular" o que não entra no mp4; nada de núcleo editado. **Restringe**: efeitos em texto/legenda e MP4 na V2 ficam preview-only e rotulados; karaokê só é "no mp4" se o burn-in for feito de verdade.
- **ADR-004** (fidelidade) + CLAUDE.md gates — legendas/transcrição não são da aula 014 (nem da 8/export: `docs/domains/export/prd.md:21` lista "Legendas automáticas (Whisper)" como fora de escopo). Tudo `[extensão]` com aprovação (o plano diz aprovada) e **ADR nova** obrigatória (serviço externo novo).
- **ADR-008** — testes sem rede/navegador; CI `make verify` (ruff `studio tests scripts` + pytest). **Restringe**: `openai` nunca chamado em teste; provider fake obrigatório; import do SDK preferencialmente lazy.
- **ADR-006** — jobs em thread + polling (`JobRegistry`); geração de legendas curta pode ser síncrona, mas whisper em arquivo grande deve considerar job.
- **ADR-003** — estado só em arquivo; `edit/narration/` novo cabe no padrão (`ingest` grava em `edit/candidates/` com `kind`).
- **ADR-010/HLD v1.2** — `steps.py` é núcleo: só frente de preparo/shell edita. A renomeação no catálogo (`steps.py:23`) viola a regra se feita pela frente A "na passada"; ver ATENÇÃO.
- **ADR-002** só proíbe API HTTP da Higgsfield — não impede OpenAI, mas a ADR nova deve dizer isso explicitamente.

## CONTRATOS EXISTENTES (`studio/etapas/edit/router.py`, prefixo `/api/projects/{pid}/edit`)
| Rota | Modelo/entrada | Saída / erros |
|---|---|---|
| `GET /api/edit/ffmpeg` :89 | — | `{available}` |
| `GET /timeline` :95 | — | `{created,duration,timeline}`; 404/422 |
| `PUT /timeline` :104 | `TimelineReq` :52-59 (`clips:[ClipReq]` :18-30 com `id?,scene,shot,take,file,in(alias src_in),out,start?,speed,blend,zoom`; `blacks:[BlackReq]`; `music:MusicReq` :38-43 `{file?,offset,volume?,muted?}`; `sfx:[SfxReq]` `{file,at,gain}`; `fade_out`; `loudnorm`; `editor: dict|None`) | `{created:false,duration,timeline}`; 404 arquivo, 422 validação/traversal |
| `POST /timeline/reset` :115 | — | idem GET |
| `POST /propose-cuts` :124 | `ProposeReq{offset?,black_dur,apply}` | 404 sem beats/clipes |
| `POST /last-frame` :133 | `LastFrameReq{scene,shot,take?}` | 409 sem ffmpeg, 404, 502 |
| `GET /sfx` :146 · `POST /sfx/upload` :152 | multipart `files: list[UploadFile]`, `prompt: Form("")`, limite 25 MB (413) → `edit.import_sfx` → `ingest.import_upload(root,"edit",files,prompt,kind="audio")` (`service.py:491-497`) | `{added}`; 422 extensão |
| `GET /media` :167 · `POST /media/upload` :173 | multipart, 200 MB; `import_media` separa image/video (`service.py:508-529`) | `{added}`; lista `{id,name,file:"edit/candidates/…",kind,duration}` |
| `POST /render` :189 | `RenderReq{target,width?,height?,fps?,quality?}` | job dict; 409 sem ffmpeg/`NO_MUSIC`/já rodando, 404 sem timeline, 422 sem clipes |
| `GET /render/job` :206 | — | `registry.status(pid)` |
Padrão de erro: `_translate` :83-86 (FileNotFoundError→404, resto→422). Front: `ui.upload(url, files, field="files", extra)` (`web/ui.js:126`), `ui.progressJob({title,subtitle,start,jobUrl,done})` (`ui.js:46`).
Extensões aceitas por `ingest.MEDIA_EXT` (`common/ingest.py:22-27`): audio `.wav/.mp3/.m4a/.ogg`, video `.mp4/.mov/.webm`. Postman: `docs/domains/edit/postman/edit.postman_collection.json` (atualizar com as rotas novas; `make qa-api` roda newman).

## CONFIG, CHAVES E FAKES
- `studio/common/settings.py` **não** lê env: é o registro de ações/modelos default + livro-caixa (ADR-016, `ACTIONS` :32, `DEFAULTS` :61, `default_for` :143). Colocar `OPENAI_API_KEY` ali destoa do módulo. Env vive em `studio/config.py:6-10` (`STUDIO_PROJECTS/MOODBOARDS/STATE`) e em módulos: `prompter.py:21` `MODEL = os.environ.get("STUDIO_PROMPTER_MODEL", …)`, `BIN` via `shutil.which`, `available()` :149. Padrão recomendado: `studio/edit/captions/__init__.py` com `WPS = 2.4` e `get_transcribe()` lendo `os.environ.get("OPENAI_API_KEY")` **em tempo de chamada** (não na importação) — assim `monkeypatch.setenv/delenv` funciona sem reimport.
- Fakes: `tests/test_prompter.py:17-57` faz `monkeypatch.setattr(prompter, "BIN", …)` e `prompter.subprocess.run`; `conftest.studio_env` :14-32 apaga `sys.modules["studio.*"]` e reimporta por teste (env lido na importação também é refeito). ffmpeg em teste: `ffmpeg_or_skip` :73-78 / `has_ffmpeg` (`test_edit_service`), `make_video/make_audio` :53-69 (lavfi). Teste de API não pode importar `openai`: o SDK deve ser importado dentro de `OpenAITranscribe` (try/except → `ProviderError`).
- Dependências: `requirements.txt` (6 linhas: playwright, fastapi, uvicorn[standard], pillow, python-multipart, numpy); `requirements-dev.txt` = `-r requirements.txt` + pytest/httpx/ruff; `pyproject.toml` **não** lista dependências (só metadata/ruff/pytest) — adicionar `openai` só em `requirements.txt`. Cada worktree tem `.venv` próprio (`make setup`).

## CATÁLOGO E TESTES QUE FIXAM TÍTULO/GUIA
- `steps.SOON` (`steps.py:23`) título "Montagem no ritmo"; `all_steps()` :34-43 faz `{**s, **plugins[id]["meta"]}` → **META do plugin vence** (`etapas/edit/__init__.py:1`). O shell (`web/app.js:212,234,288`) e o guia (`common/guide.py:167,222`, `etapas/edit/guide.py:10,48` usa `META`) leem `title` daí. Logo mudar só `__init__.py` já renomeia UI + guia; `steps.py` é cosmético/consistência, mas é núcleo (regra preparo/shell).
- `tests/test_steps_and_config.py:19-22` compara só `META.n` e `META.aula` com `SOON` (título livre). `tests/test_edit_guide.py:33` fixa `n==7`, `aula=="014"`, `next_step=="export"`, textos do guia :34-36 (não tocar `guide.py`).
- `tests/test_edit_api.py:316-331` exige em `view.html`: `"Etapa 7 · aula 014"` (:322 — manter o eyebrow), `"[extensão]"`, a string exata `'<section id="guide" class="guide ved-fallback"></section>'` (:326), `class="ved"`/`id="ved"`; em `view.js`: `Studio.register("edit"`, `Studio.ui`, `destroy()`, `onProject()`, `ctx.guide()`. `:333-350` exige fontes, `--vac:#4FC8D9`, `--vbg`, ausência de `crimson`, helpers `ui.modal( ui.drop( ui.upload( ui.progressJob(`, `openGuide` e `"aula 014"` em js. Nenhum teste fixa "Montagem no ritmo".
- Outras ocorrências do nome a alinhar: `README.md:76`, `view.js:428,458,1000`, `view.html:257-258`, docstrings `studio/edit/__init__.py:1`, `service.py:1`, `router.py:1`, docs (`edit/prd.md:1`, `edit-fdd.md:1`, `edit-fluxo.md:1`, `wave-1.md:100`, ADR-030:10 — histórico, não reescrever).

## SUPERFÍCIE DE CONFLITO ENTRE A / B / C
| Arquivo | A editor-estavel | B legendas-backend | C legendas-frontend |
|---|---|---|---|
| `studio/edit/editor.py` `normalize_item` :261-265 | + `effects/filters/presetCss` em text/caption; `ui.tlHeight` :425 | + `words/mode/hi/chunk` em caption (+ `CAPTION_MODES`) | — |
| `tests/test_edit_editor.py` | novos testes (round-trip fx, tlHeight, clips vazios) | novos (words/mode/hi, PNG por palavra) | — |
| `tests/test_edit_api.py` | 316/333 conferir; PUT sem música/sem clipes; render 422 | generate/upload 422/404/estimate | — |
| `studio/edit/burnin.py` | — | `render_layer_pngs` por palavra/linha | — |
| `studio/etapas/edit/router.py` | — | 2 rotas + modelos | — |
| `studio/etapas/edit/view.js` | quase todo (commit/renderDirty, renderLayers reconciliado, deleteItems, moveToTrack, cssFilterFor, header) | — | `pCaptions` :550-557, `propsTextBody` :710-730, `renderLayers` (spans), `loopTick/seekTo` (paintKaraoke), constantes `CAP_PRESETS/HI_COLORS/CHUNK_OPTS/WPS` |
| `view.html` | CSS :137, :205, :223, keyframes, `.app.side-hidden` | — | CSS das spans de karaokê |
| `steps.py`, `etapas/edit/__init__.py`, `README.md` | rename | — | — |
| `requirements.txt`, `config`/captions env | — | `openai` + chave | — |
| `docs/domains/edit/features/editor-video-completo-fdd.md` | decisões de comportamento | nota legendas + contrato | idem UI |
| `docs/adrs/` | nenhuma ADR | ADR nova + `mapping.md` | — |

Recomendação de ordem: **A e B em paralelo** (interseção real = 5 linhas de `editor.py` + arquivos de teste, ambos aditivos — combinar que B escreve um helper separado `normalize_caption_extra(raw)` chamado no branch caption e A só acrescenta as 3 linhas de fx; testes em funções novas com nomes distintos); **C depois de A e B integrados**, pois depende de `renderLayers` reconciliado por `data-uid` (item 1 de A), de `renderDirty({panel:true})` e do contrato de `POST /captions/generate`. Congelar antes de largar C: schema do item (`words:[{w,start_s,end_s}]`, `mode`, `hi`, `chunk`), resposta `{items, source, word_count}`, `WPS=2.4`. Portas: A `8766`, B `8767`, C `8768` (`PORT=… ./run.sh`, `run.sh:5`).

## LACUNAS E DESATUALIZACOES
- **Não há "roteiro" no Studio.** O mais próximo é `text` por cena em `storyboard/scenes.json` (`GET /api/projects/{pid}/storyboard/scenes` → `{scenes:[{id,n,text,images,primary}]}`, `storyboard/service.py:416-438`) — descrição visual, não narração. Não há narração/voz no método (áudio dos takes sai `sound:False`; aula 014 usa trilha + SFX). A fonte "áudio com fala" na prática será só o upload novo; o pré-preenchimento com descrições das cenas produz legenda de "descrição", não de fala — decisão de produto a rotular na UI.
- **Extração de áudio**: inexistente em `common/ffmpeg.py`; whisper aceita ≤25 MB → extrair para wav/mp3 16 kHz mono em pasta temporária (scratch do projeto `edit/_narration/` ou `tempfile`).
- **Burn-in por palavra**: `render_layer_pngs` gera PNG full-frame por item e o filtergraph cria um `-i` + um `overlay` por PNG (`render.py:242-245,277-280`). 60 s de karaokê a 2,4 wps = ~145 inputs; não existe fallback `ffconcat`/faixa; implementar o limiar (~200) do plano ou aceitar o custo. O `_overlays` é limpo no fim — ok.
- **MP4 na VÍDEO 2 no master**: `_image_png` falha para vídeo (`burnin.py:113-117`) e o FDD §3 já registra como "fase 3 pendente". O item 4 do plano é **preview-only**; o rótulo "no master.mp4: fase seguinte" precisa continuar visível.
- **Efeitos em texto/legenda no ffmpeg**: só `_clip_fx_chain` em V1; a política do FDD ("preview, nunca simular") manda rotular.
- **`ui`** aceita só `zoom/snap` (`editor.py:425-426`); `tlHeight/leftW/rightW` exigem código + teste ao lado de `test_ui_zoom_is_a_factor` (:226).
- **`deleteItems` para SFX** filtra por índice `sfx_${i}` dentro de um `forEach` (`view.js:924`): excluir 2 SFX de uma vez desloca índices — bug latente a corrigir junto com a exclusão da música.
- **Sidebar**: `.app{grid-template-columns:var(--side-w) minmax(0,1fr)}` (`web/style.css:266`, `--side-w:264px` :40) e `.ved` fixo com `left` calculado em `fit()`. CSS global mínimo em `view.html` como o plano prevê; `test_edit_api.py:341` proíbe apenas "crimson".
- **Fontes do Pillow**: `_font` ignora `style.font`; karaokê/`draw_caption` herdam Liberation/DejaVu — largura real medida com essa fonte, não com a do preview (pequena divergência preview × mp4).
- **ADR numbering**: em disco existem ADR-001…023 e ADR-030 (o "30" veio do seq do Task-Id `ADH-OS-20260828-30`, não da sequência); `docs/adrs/mapping.md` registra até ADR-023 (`:648`) e **não cita ADR-030**; `docs/adrs/README.md` indexa só até ADR-015 (101 linhas). Próximo número sequencial pela mapping = **ADR-024**; alternativa "não colidir com 030" = ADR-031. **[decisão da wave: ADR-024, sequencial; ADR-030 é indexada retroativamente na mapping pela frente B.]**
- Docs desatualizadas: `edit-fdd.md:1`, `prd.md:1`, `edit-fluxo.md:1` chamam de "Etapa 8" (renumeração ADR-015 → é etapa 7); `docs/adrs/README.md` sem ADR-016+; FDD do editor §3 diz "overlays de vídeo só imagem" — continua verdade após a wave.
- Trello: board `senhordatecnologia` com listas `DD ·` (criadas na W0 desta wave; ver `docs/dd.md`).

## ATENCAO PARA ESTE TRABALHO
1. **`renderRoot()` só em `load()`/reset/troca de projeto**: qualquer `commit` que ainda cair em `renderRoot` reintroduz o bug 1; `adopt()` (`view.js:370`) precisa continuar preservando identidade — reconciliação por `data-uid` deve usar `o.id`, nunca índice.
2. **Não quebrar `test_edit_api.py:316-350`**: manter "Etapa 7 · aula 014" no eyebrow, a `<section id="guide" …>` literal, `"aula 014"` e `openGuide` em `view.js`, helpers `ui.*`, sem "crimson".
3. **Renomear = tocar `steps.py` (núcleo)**: **[decisão da wave: a frente A assume o papel de "preparo" nessa linha (META + `steps.py`), registrado no PR.]**
4. **Backbone intacto**: `validate_timeline`/`build_filtergraph` sem clipes já dão 422 (`render.py:184-185,356-357`) — a guarda vai para `startRender()` no front; PUT com `clips:[]` e `music.file:null` já passa (fixar em teste, não "corrigir").
5. **ADR nova para whisper é obrigatória** (serviço externo novo; ADR-008 sem rede; ADR-002 não cobre OpenAI): fake sem chave, `source:"estimate"`, import lazy do SDK; chave lida em runtime, não em `settings.py` (ADR-016 é outro assunto).
6. **Perguntas que a entrevista de FDD pode pular** (já respondidas pelo código/FDD): timeline vazia é válida (`empty_timeline`, `service.py:358-366`); trilha removível (`music.file` nullable, `router.py:39`); efeitos em V1 entram no mp4 só blur/sharpen/grain (`render.py:129-139`); overlays só imagem no master (FDD §3); `ui.zoom` é fator 0.25–4.
7. Gitflow: worktree `../orquestrador-studio-worktrees/<branch>` (só uma ativa hoje: `feature/adh-os-20260827-13-storyboard`), branch `<tipo>/adh-os-20260829-38-…`, trailer `Task-Id:` (hook `.githooks/commit-msg`), gate `.agents/gates/ft-pr.md` + `.claude/skills/ft-pr/SKILL.md`, base `develop`, `make verify` antes do PR; IDs `ADH-OS-20260829-38/39/40` livres.

## ARQUIVOS-FONTE
`.claude/plans/2026-08-29-studio-de-video-estavel.md`; `docs/domains/studio/recon-wave-7.md`, `hld.md`; `docs/domains/edit/prd.md`, `features/edit-fdd.md`, `features/editor-video-completo-fdd.md`; `docs/adrs/generated/STUDIO/ADR-030-*.md`, `docs/adrs/mapping.md` (600-651), `docs/adrs/README.md`; `docs/gitflow.md`, `docs/dd.md`, `docs/dd-parallel.md`, `docs/domains/export/prd.md:21`; `CLAUDE.md`; `Makefile`, `requirements*.txt`, `pyproject.toml`, `run.sh`, `.githooks/commit-msg`, `.agents/gates/ft-pr.md`; `studio/etapas/edit/{__init__.py,router.py,view.js,view.html,guide.py(10,48)}`; `studio/etapas/__init__.py`; `studio/edit/{editor.py,service.py,render.py,burnin.py}`; `studio/common/{settings.py,ffmpeg.py,prompter.py(140-200),ingest.py(20-118),guide.py(167,222)}`; `studio/config.py`; `studio/steps.py`; `studio/web/{style.css(40,266),app.js(212-288),ui.js(46,126)}`; `studio/storyboard/service.py(416-438)`; `tests/{conftest.py,test_edit_api.py,test_edit_editor.py,test_edit_guide.py,test_steps_and_config.py,test_prompter.py,test_edit_service.py(32)}`.
