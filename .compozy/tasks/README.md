# Workflows SDD (Compozy)

Um diretório por workflow: `.compozy/tasks/<slug>/` com `_prd.md`, `_techspec.md` (o FDD copiado), `_tasks.md` e `task_NN.md`. Handoff e reconciliação: `~/.claude/skills/dd/references/compozy.md`.
