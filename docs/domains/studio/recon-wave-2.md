# Recon — Wave 2 (guia por etapa, shell profissional, dashboard, fidelidade ao roteiro)

Gerado por `dd-parallel-recon` em 2026-08-25 a partir de `develop` (HEAD `367a56c [OS-012]`).
Estado compartilhado da wave 2: **todas as frentes leem este arquivo em vez de reexplorar o codebase.**

## TERRENO

- Raiz: `/home/arthu/code/senhortecnologia/orquestrador-studio` (branch `develop`; árvore limpa). `docs/domains/` existe para os 13 domínios; HLD só em `studio` (v1.1), `higgsfield`, `refs`, `mood`, `base`, `animate`; os demais só têm `prd.md` + `features/<id>-fdd.md` + `diagrams/mermaid/` + `postman/`. Contratos entre etapas: `docs/domains/studio/waves/wave-1.md` (provides/consumes + 16 decisões do lote); retro em `waves/wave-1-retro.md`; API transversal em `waves/wave-1-api-transversal.md`; recon anterior em `docs/domains/studio/recon-wave-1.md`.
- **Núcleo (`studio/app.py`, 86 linhas)**: `PLUGINS = discover()` → `include_router` de cada plugin; rotas próprias: `GET /api/steps` (`steps.all_steps()`), `GET/POST /api/projects` (`refs.service.list_projects/create_project`; 409 se existe), `GET /api/higgsfield/status` (`hf.status()` = subprocess `account status`, timeout 30 s, sem cache), `GET /steps/{step_id}/{asset}` (só `view.html`/`view.js` da pasta do plugin — nenhum outro asset por plugin é servido), mounts `/files` (PROJECTS_DIR) e `/static` (WEB_DIR), `GET /` → index.html. Handler global `KeyError` → 404. Não há `GET /api/projects/{pid}` individual; `project.json` = `{id, name, product, vibe, created}` (`refs/service.py:34-43`).
- **Catálogo (`studio/steps.py`)**: `SOON` com as 11 etapas (id, n, title, aula, desc); `all_steps()` faz `{**SOON, **plugin.meta}` (status `ready`) ou `status: "soon"`. **Descoberta (`studio/etapas/__init__.py:discover`)**: exige `META.id == nome da pasta`; `router` opcional; devolve `{id: {meta, router, dir}}` ordenado por `n`. Teste `tests/test_steps_and_config.py:78-88` exige `ready == set(discover())` e `META.n/aula == SOON` → **não é possível criar um pseudo-plugin transversal (ex. `etapas/guide/`) sem tocar `steps.py` e esse teste**.
- **Layout (`studio/config.py`)**: `PROJECT_LAYOUT` = `refs/candidates(/thumbs)`, `refs/brainstorming`, `mood`, `assets`, `images`, `videos`, `audio`, `edit`, `export`, `jobs`. Pastas `base/`, `storyboard/`, `shots/`, `animate/`, `publish/`, `prospect/`, `mood/vibe/` são criadas pelos serviços. `music` grava em `audio/` (`music/service.py:29 STEP="audio"`), id da etapa é `music` — o guia precisa desse mapeamento.
- **Shell web (`studio/web/`)**: `index.html` (38 linhas: `aside.side` com `select#projSel`, `form#newProj` [name, product, vibe], `nav ol#steps`; `main#main`; `#toast`); `app.js` (77 linhas): `api(path, opts)` (JSON, lança `Error(detail)`), `toast`, `window.Studio = {register(id, factory), ctx: {$, api, toast, pid(), project(), files(rel)}}`; `showView(id)` faz `fetch(/steps/<id>/view.html)` → `main.innerHTML`, carrega `view.js` uma vez (`loaded` Set), **recria a instância a cada exibição** (`instances[id] = factories[id](ctx); init()`), sem `destroy()` — timers de polling da instância anterior (`setTimeout(poll, 3000)` em todos os plugins) continuam vivos; menu = `li.ready|soon|active` sem status por etapa; `localStorage` `studio.pid`/`studio.view`; sem roteamento por URL. `style.css` (72 linhas, vars CSS + dark mode). Fontes via Google Fonts (única dependência de rede da UI).
- **Padrão dos 11 plugins** (`studio/etapas/<id>/{__init__.py, router.py, view.html, view.js}` + `studio/<id>/service.py`): `view.html` começa com `<header class="stephead"><span class="eyebrow">Etapa N · aula X</span><h2>…</h2><p class="lede">…</p></header>` e `section.panel` numeradas; `view.js` = `Studio.register("<id>", ctx => ({init(){…; this.onProject()}, async onProject(){ if(!ctx.pid()) return; … }}))`. Todos usam só `ctx.pid/ctx.files/$/api/toast` (refs usa `ctx.project()`). **Código duplicado por plugin** (candidato a `Studio.ui`): `esc()` em 7 plugins; `hfStatus()` chip CLI em base/mood/storyboard/shots/animate/music (+export via `st.higgsfield`); `downloads-folder` em mood/base/shots/animate/music (cada um com rota própria `GET /api/<id>/downloads-folder`); drag&drop upload (`fetch` multipart cru) em 7; `confirm()` de custo antes de gerar em 8; polling 3 s; chips "conclua a etapa N" hardcoded (`grep 'etapa [0-9]' studio/etapas/*/view.html`). Tamanhos: view.js 79–258 linhas, view.html 39–108, router 59–253, service 148–526 (total 8.354 linhas). IDs de DOM repetidos entre plugins: `btnSave` (refs, edit), `baseUpload`/`baseDrop` (2×) — inofensivo hoje (uma view por vez), colide se um dashboard montar mais de uma.
- **`studio/common/`**: `ingest.py` (`ingest_bytes/load_candidates/save_candidates/import_upload/import_downloads/import_history`, `MEDIA_EXT`, `DOWNLOADS_DEFAULT`), `jobs.py` (`JobRegistry.start/status`; instância por módulo de serviço; **`refs/service.py` ainda usa `_jobs/_lock` próprios**), `ffmpeg.py` (`available/run/probe/last_frame/video_thumb`), `prompter.py` (OS-012: `claude -p`, `ROLES mood|base|motion`, `from_brief/from_images/fallback_template/enforce_mood_rules`). Não existe nada de "guia", "status transversal" ou "dashboard".
- **Como cada etapa sabe hoje o que está pronto** (checagem de existência de arquivo espalhada por serviço; sem contrato comum):
  - refs: `refs/candidates/candidates.json[].selected` → `refs/brainstorming/*.jpg` + `refs/README.md` (só `GET .../refs/candidates`).
  - mood: `mood/candidates.json`, `mood/selected/*`, `mood/palette.json`, `mood/mood.md`, `mood/vibe/candidates.json`, `mood/prompts.json` (`GET .../mood/candidates`, `.../mood/vibe`).
  - base: `base.final_file()` → `base/base_final.png`; `GET .../base/candidates` → `{candidates, final}`; `brand_get/brand_set`, `base/base.md`.
  - storyboard: `sb.status()` em `GET /api/projects/{pid}/storyboard` → `{base_image, has_base, ideas, selected, scenes, scenes_with_text, storyboard_md}`; `Precondition` (409) sem `base/base_final.png`.
  - shots: `GET .../shots/scenes` → `{warning, palette, base_final, scenes[{id,n,text,image,base,base_ready,candidates,selected}], product_scene{ref_ready,selected}}` (404 sem `storyboard/scenes.json`); `GET .../shots/storyboard` 404 até a 1ª seleção.
  - animate: `GET .../animate/shots` → `{shots[{…, ready, takes}], ready, total, model_order, warnings}` (404 sem `shots/storyboard.json`); **`load_plan` grava `animate/takes.json` (side effect)**.
  - music: `GET .../music/candidates`; `GET .../music/beats` 404 até `select` (grava `audio/music.<ext>`, `audio/beats.json`, `audio/license.txt`).
  - edit: **`GET .../edit/timeline` cria e grava `edit/timeline.json` na 1ª chamada** (404 sem takes/storyboard, 422 sem take liked); `GET /api/edit/ffmpeg`; `GET .../edit/render/job`.
  - export: `GET .../export/status` → `{ffmpeg, higgsfield, master{exists,file,+probe}, outputs{16x9|9x16|1x1|thumb|qa_report}, previews, job}` (chama `ffprobe` por arquivo existente).
  - publish: `GET .../publish/portfolio` → `{count, distinct_videos, goal:4, ready, missing, portfolio_md}`; `GET .../publish/exports`.
  - prospect: `GET .../prospect/gate` → `{published, posts, required:4, ok, message, today_sent, daily_limit:10}`.
- **Testes**: 392 coletados, 27 arquivos em `tests/`. `conftest.py`: `studio_env` (seta `STUDIO_PROJECTS/STATE/DOWNLOADS`, reimporta `studio*`; devolve `{tmp, app, refs, mood, svc(name)}`), `client` = `TestClient`, helpers `make_image/image_bytes/make_video/make_audio` (ffmpeg lavfi; CI instala ffmpeg). CLI Higgsfield sempre fakeado. `make verify` = `ruff check studio tests scripts` + `pytest`. **Frontend: zero testes de JS/DOM**; só asserts HTTP: `test_steps_and_config.py:97-103` (todo plugin serve `view.html`/`view.js` 200), `test_api.py:6` (`GET /` 200), e **strings fixadas**: `test_base_api.py:38-39` (`"Etapa 3 · aula 009"`; `'Studio.register("base"'`), `test_publish_api.py:163-171` (`"Etapa 10 · aula 015"`, `"4 vídeos"`, `"feedback"`; ids exatos `{pubVideo, pubNetwork, pubDate, pubUrl, pubNote}`; `"distinct_videos"` no js), `test_music_api.py:28-29`. `scripts/crossfeature_wave1.py` percorre 3→11 pela API no projeto `2026-08-wave-teste`.
- Projetos locais: `projects/2026-08-gelo-zero` (refs/mood parciais) e `projects/2026-08-wave-teste` (todas as pastas com fixtures — bom para smoke).

## DECISÕES VIGENTES

- ADR-001 monólito single-process, sem auth, bind `127.0.0.1`. Google Fonts é a única saída de rede da UI.
- ADR-002 Higgsfield só via CLI — chip de status/créditos vem de `hf.status()` (subprocess).
- ADR-003 persistência em FS sob `projects/<id>/` — guia/dashboard derivam estado **lendo arquivos**.
- ADR-004 fidelidade ao roteiro — textos do guia vêm da aula (`docs/plano/plano-automacao-videos.md` §1.1 + `steps.py` desc + `lede`); "próxima ação" = ordem das aulas; wizard só com `name, product, vibe`; campos novos = `[extensão]`.
- ADR-006 jobs em thread + polling — `job.state` some no reinício.
- ADR-007 mood vibe única, teto 8.
- ADR-008 testes sem rede/navegador, CI ruff+pytest — **teste de frontend com Node/Playwright contraria a ADR; UI é testada por asserts HTTP/strings ou exige ADR novo**.
- ADR-009 batidas numpy+ffmpeg.
- Decisões do lote wave-1 a espelhar no guia: #1 gate de prospect = `distinct_videos >= 4`; #5 master 1920×1080/30 fps; #6 edit monta sem `beats.json` (warn, não fail); #7 `storyboard/ideas/` só selecionadas; #15 implementação direta por plugin.
- **Regra do HLD studio v1.1** ("etapa nova nunca edita `app.py`, `index.html`, `app.js`, `steps.py`") é rompida por definição pela feature `shell`: exige HLD studio v1.2 + nota de que só a frente de preparo/shell toca esses arquivos.

## CONTRATOS EXISTENTES (prefixo `P = /api/projects/{pid}`)

- núcleo: `GET /api/steps`; `GET/POST /api/projects`; `GET /api/higgsfield/status` `{installed, logged_in, plan?, credits?, email?, error?}`; `GET /steps/{id}/view.{html,js}`; `/files/{pid}/…`; `/static/…`.
- refs: `GET /api/suggest-terms?product&vibe`; `POST|GET /api/pinterest/login`; `POST P/refs/search {terms,max_per_term,headless}`; `GET P/refs/job`; `GET P/refs/candidates`; `POST P/refs/select {ids,notes}`. Provides `refs/candidates/candidates.json`, `refs/brainstorming/*.jpg`, `refs/README.md`.
- mood: `GET P/mood/prompts?model&variation`; `GET P/mood/vibe`; `POST P/mood/vibe/import/{upload,downloads}`; `POST P/mood/prompts/generate`; `GET P/mood/prompts/history`; `GET P/mood/candidates`; `POST P/mood/import/{upload,downloads,history}`; `GET /api/mood/downloads-folder`; `POST P/mood/{cost,generate}`; `GET P/mood/job`; `POST P/mood/select {ids≤8,note}`. Provides `mood/candidates.json`, `mood/selected/*`, `mood/palette.json {colors[≤6],note}`, `mood/mood.md`.
- base: `GET P/base/prompts?model`; `GET|POST P/base/brand`; `GET P/base/candidates` → `{candidates,final}`; `POST P/base/import/{upload,downloads,history}` (kind situation|label|upscale); `POST P/base/{cost,generate}`; `GET P/base/job`; `POST P/base/select {id,note}`. Provides `base/candidates.json`, `base/base_final.png`, `base/base.md`.
- storyboard: `GET P/storyboard`; `GET|POST P/storyboard/instructions`; `POST P/storyboard/import/{upload,downloads,history}`; `GET P/storyboard/candidates`; `POST P/storyboard/candidates/select`; `GET|PUT P/storyboard/scenes`; `POST P/storyboard/render`; `POST P/storyboard/{cost,generate}`; `GET P/storyboard/job`. Provides `storyboard/scenes.json {scenes[{id:cenaNN,n,text,image}]}`, `storyboard/ideas/`, `storyboard/candidates/`, `storyboard/storyboard.md`.
- shots: `GET P/shots/scenes`; `POST P/shots/scenes/{scene}/base {source}` | `/base/upload`; `GET .../{scene}/prompts`; `POST .../{scene}/import/{upload,downloads,history}`; `GET .../{scene}/candidates`; `GET /api/shots/downloads-folder`; `POST .../{scene}/{cost,generate,upscale,select}`; `GET P/shots/job`; `GET P/shots/storyboard`; produto: `POST P/shots/product/ref`, `GET .../product/prompts|candidates`, `POST .../product/import/*|cost|generate|upscale|select`. Provides `shots/storyboard.json`, `shots/cenaNN/base.png|shotMM_final.png`.
- animate: `GET P/animate/shots`; `PUT P/animate/shots/{scene}/{shot}`; `GET P/animate/prompt?…`; `GET P/animate/candidates`; `POST P/animate/import/{upload(≤200 MB),downloads,history}`; `GET /api/animate/downloads-folder`; `POST .../{scene}/{shot}/takes` (201); `POST .../takes/{take}/like`; `POST P/animate/{cost,generate(202)}`; `GET P/animate/job`. Provides `animate/takes.json`, `videos/cenaNN/shotMM_takeK.mp4`.
- music: `GET P/music/prompt`; `GET P/music/candidates`; `POST P/music/import/{upload,downloads,history}`; `GET /api/music/downloads-folder`; `POST P/music/generate/cost`, `POST P/music/generate` (202), `GET P/music/generate/job`; `POST P/music/select {id,license}`; `GET|POST P/music/beats`. Provides `audio/candidates.json`, `audio/music.<ext>`, `audio/beats.json {bpm,beats,impacts,duration}`, `audio/license.txt`.
- edit: `GET /api/edit/ffmpeg`; `GET|PUT P/edit/timeline`; `POST P/edit/timeline/reset`; `POST P/edit/propose-cuts {offset,black_dur,apply}`; `POST P/edit/last-frame`; `GET P/edit/sfx`; `POST P/edit/sfx/upload`; `POST P/edit/render {target: rough|master}`; `GET P/edit/render/job`. Provides `edit/timeline.json`, `edit/rough_cut.mp4`, `edit/master.mp4`, `edit/last_frames/*.png`, `edit/candidates/`.
- export: `GET P/export/status`; `GET P/export/list`; `POST P/export/preview`; `POST P/export/render {formats}`; `GET P/export/job`; `POST P/export/thumb`; `POST P/export/qa`; `POST P/export/reframe/cost|reframe`. Provides `export/{16x9,9x16,1x1}.mp4`, `export/thumb.jpg`, `export/qa_report.md`, `export/previews/*.jpg`, `export/.state.json`.
- publish: `GET P/publish/exports`; `GET P/publish/log` → `{posts,count,distinct_videos,goal}`; `POST P/publish/log` (201); `POST P/publish/log/{post_id}/feedback`; `DELETE P/publish/log/{post_id}`; `GET P/publish/portfolio`. Provides `publish/log.json`, `publish/portfolio.md`.
- prospect: `GET P/prospect/gate`; `GET|POST P/prospect/leads`; `GET|PUT|DELETE P/prospect/leads/{lid}`; `GET .../{lid}/dm`; `POST .../{lid}/sent|replied|teaser|call`; `GET .../{lid}/followup`; `GET P/prospect/job`; `GET|POST P/prospect/pitch`. Provides `prospect/leads.json`, `prospect/teasers/<lead>.mp4`, `prospect/pitch.md`.
- Erros padronizados: `KeyError`→404 (núcleo), `FileNotFoundError`→404, `ValueError`→422, `RuntimeError`→409 (job/CLI/gate), CLI falho em import→502, upload >25 MB→413 (animate 200 MB).

## LACUNAS E DESATUALIZAÇÕES

- `docs/domains/studio/hld.md` §"Interfaces públicas" ainda lista só `/refs/*` e `/mood/*`. `docs/adrs/mapping.md` diz v0.2.0 e "só etapas 1 e 2" (pyproject 0.3.0, 11 etapas). Relatório `docs/agents/component-deep-analyzer/component-analysis-Web-Frontend-*.md` descreve o `app.js` pré-plugins — obsoleto.
- `steps.py` desc de `export` diz "9:16 / 1:1, legendas, checklist" — legendas não existem (export-fdd marca `captions.srt` como inferência); export mostra "aulas 007 e 014" enquanto `META.aula="014"`.
- Sem contrato de status/prontidão transversal. Duas leituras "de status" têm **efeito colateral de escrita**: `edit.get_timeline` e `animate.load_plan` — um guia não pode chamá-las para checar prontidão.
- `hf.status()` dispara subprocess (até 30 s) sem cache; 7 plugins chamam a cada `onProject`. `export.status()` roda `ffprobe` por arquivo.
- `app.js` sem `destroy()`/cancelamento de polling; sem tratamento de erro em `showView`; `loaded` Set impede recarregar view.js; sem roteamento por URL.
- Frontend sem teste automatizado próprio; sem lint de JS/CSS.
- `PROJECT_LAYOUT` não cobre as pastas das etapas 3–6/10/11 (ampliar é seguro; teste fixa só 5 entradas).
- HLD ausente para storyboard, shots, music, edit, export, publish, prospect.
- `refs/service.py` não migrou para `JobRegistry`.
- Trello: board `orquestrador-studio` ainda não criado.

## ATENÇÃO PARA ESTE TRABALHO

- **Mapa de conflito de arquivos**: (a) `guide` → novo `studio/common/guide.py` + 1 hook por plugin (11 pastas) + rota transversal em `studio/app.py` (ou descoberta em `etapas/__init__.py`) + `tests/test_guide*.py`; (b) `shell` → `studio/web/{index.html,app.js,style.css}` exclusivos; (c) `dashboard` → depende de (a) e (b); se for tela do núcleo, mexe nos mesmos arquivos de (b) → **fundir em `shell`**; (d) revisão de fidelidade → `view.html/view.js/router/service` das 11 etapas (**mesmas pastas do hook de (a)**). Ordem: preparo do orquestrador (contrato do guia + `Studio.ui` mínimo + HLD v1.2) → `guide` e `shell` em paralelo → revisão por etapa consumindo `Studio.ui` e `guide` já mergeados.
- Contrato de plugin a preservar: `META`, `router` opcional, `Studio.register(id, ctx => ({init, onProject}))`, `ctx.{$,api,toast,pid,project,files}`; `GET /steps/{id}/{view.html|view.js}`. Componentes compartilhados vivem em `studio/web/` (servidos em `/static`).
- Strings fixadas por teste que o redesenho não pode quebrar: `"Etapa 3 · aula 009"` e `'Studio.register("base"'`; `"Etapa 10 · aula 015"`, `"4 vídeos"`, `"feedback"`, ids exatos `{pubVideo,pubNetwork,pubDate,pubUrl,pubNote}` e `"distinct_videos"`; `/steps/<id>/view.{html,js}` 200; `META.n/aula == SOON`; `GET /` 200. Suíte deve seguir em 392+ verdes, `ruff` limpo (E,F,W,I,B; linha 120).
- Prontidão calculada por **leitura pura de arquivos** (reaproveitar `base.final_file`, `storyboard.status`, `shots.list_scenes`, `music._music_path`/`_beats_file`, `edit.load_timeline` (não `get_timeline`), `export.status` sem probe, `publish.portfolio_status`, `prospect.gate`; animate lê `animate/takes.json` direto). Evitar `hf.status()`/`ffprobe` dentro do guia; se o dashboard precisar de CLI/ffmpeg, uma chamada cacheada.
- Fidelidade (ADR-004): guia = explicação da aula + pré-requisitos + o que falta + próxima etapa — nada de checklist que a aula não ensina; wizard só com name/product/vibe; progresso derivado dos artefatos, não campo novo em `project.json`.
